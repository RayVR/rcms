[package]
name = "rcms"
version = "0.0.0"
edition = "2021"
publish = false

[features]
default = ["std"]
std = []
file-io = ["std"]
native-floor = []

[dev-dependencies]
rcms-oracle = { path = "../rcms-oracle" }
